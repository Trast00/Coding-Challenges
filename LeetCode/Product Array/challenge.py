class Solution(object):
    def productExceptSelf(self, nums):
        """
        :type nums: List[int]
        :rtype: List[int]
        """
        # create a result_hash, hashmap with: key=> index, and value => products
        # for each num in nums
            # if num exist in result_hash
                # result_hash[i] = 
            # else 